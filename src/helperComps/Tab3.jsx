
function Tab3() {
    console.log('Tab3 Loaded');
    return (
        <h2>
            It improves performance especially when tabs contain heavy data,
            large components, API calls, charts, or forms. So instead of loading
            everything upfront, we load only what the user actually needs.
        </h2>
    );
}

export default Tab3;
